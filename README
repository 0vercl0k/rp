=== CMAKE ===
Pour build sous unix:
	cd build/
	cmake .. && make

=== GIT ===
Pour recuperer les files

$ git pull

Pour ajouter sont taff:

$ git add .
$ git commit -m "ton commentaire sur ce que tu as apporté"
$ git pull
$ git push


Voila c'est le commande de base, normalement il n'y a pas besoin de plus.
